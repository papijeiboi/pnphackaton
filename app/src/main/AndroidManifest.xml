<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jemoje.pnphackaton">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.QrStaffSuccessActivity"></activity>
        <activity android:name=".activities.ScanQrStaffActivity" />
        <activity android:name=".activities.DisplayPlateActivity" />
        <activity android:name=".activities.SearchPlateNumberActivity" />
        <activity android:name=".activities.ScanQrActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.PoliceMenuActivity" />
        <activity android:name=".activities.StaffMenuActivity" />
        <activity android:name=".activities.MainMenuActivity" />
        <activity android:name=".activities.LandingActivity" />
        <activity android:name=".activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>